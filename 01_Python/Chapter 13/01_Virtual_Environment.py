# Virtual enivornment means if we want to install modules but same module with different version so when I install first version and after when I install second version so first version will automatically removes so what I do for getting all the version of the module at same time.
# ANSWER :  We can create one virtual environment which is made from the copy of the our python installation and we add all the version of the module and that copy have not any relation with the original python installation means we can create one isolated environment. 

# In VS Code's terminal we can not seen any env before the path because of we already activate it and thus that's the reason.
