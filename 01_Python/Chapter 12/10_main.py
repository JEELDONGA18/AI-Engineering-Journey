from module import greet   
# In output the module is shown because of in the imported file there if one print with __name__ and that is import here so here that convert into that's file name(means module) 
# so output is basically 'module' is shown.